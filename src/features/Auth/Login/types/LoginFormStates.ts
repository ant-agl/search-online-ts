export interface AuthFormState {
    email: string;
    password: string;
}

export interface EnterCodeFormState {
    code: string;
    email: string;
}

export interface ResetPasswordFormState {
    email: string;
}
